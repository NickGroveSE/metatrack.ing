---
const backendUrl = import.meta.env.PUBLIC_BACKEND_URL;
---

<html>
<body>
  <h1>Environment Test</h1>
  <p>PUBLIC_BACKEND_URL: {backendUrl || 'NOT SET'}</p>
  <p>All env vars: {JSON.stringify(import.meta.env)}</p>
</body>
</html>